<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!-- Allow JIT compilation (required for JavaScript engine) -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>

  <!-- Allow unsigned executable memory (SpiderMonkey WASM/JIT) -->
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- Allow DYLD environment variables (used by the browser launcher) -->
  <key>com.apple.security.cs.allow-dyld-environment-variables</key>
  <true/>

  <!-- Disable library validation so browser plugins/extensions load -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <!-- Network client access -->
  <key>com.apple.security.network.client</key>
  <true/>

  <!-- Network server (for WebRTC local signalling) -->
  <key>com.apple.security.network.server</key>
  <true/>

  <!-- Camera and microphone (for WebRTC) -->
  <key>com.apple.security.device.camera</key>
  <true/>
  <key>com.apple.security.device.microphone</key>
  <true/>

  <!-- Location (for Geolocation API) -->
  <key>com.apple.security.personal-information.location</key>
  <true/>

  <!-- Addressbook/Contacts (optional, only if browser exposes it) -->
  <!-- <key>com.apple.security.personal-information.addressbook</key> -->
  <!-- <true/> -->
</dict>
</plist>
